[build]
  base = "artifacts/influencer-hub"
  command = "npm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"

# SPA client-side routing for React/Wouter router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

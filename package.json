{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@9.15.4",
  "license": "MIT",
  "scripts": {
    "dev": "powershell -File ./start-local.ps1",
    "build": "pnpm --filter @workspace/db run build && pnpm --filter @workspace/api-zod run build && pnpm --filter @workspace/frontend run build && pnpm --filter @workspace/api-server run build",
    "typecheck": "pnpm --filter @workspace/db run build && pnpm --filter @workspace/api-zod run build && pnpm --filter @workspace/frontend run typecheck && pnpm --filter @workspace/api-server run typecheck",
    "test": "pnpm --filter @workspace/api-server run test",
    "db:push": "pnpm --filter @workspace/db run push",
    "db:push-force": "pnpm --filter @workspace/db run push-force",
    "db:seed": "pnpm --filter database exec tsx ./seeds/seed.ts"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "@rollup/rollup-win32-x64-msvc": "4.62.3",
    "lightningcss-win32-x64-msvc": "1.32.0",
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
